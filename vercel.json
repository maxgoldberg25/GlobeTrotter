{
  "buildCommand": "prisma generate && prisma migrate deploy && next build",
  "ignoreCommand": "if [ \"$VERCEL_ENV\" != \"production\" ]; then echo \"Skip build step in non-production phase\"; exit 0; fi",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": ".next"
} 